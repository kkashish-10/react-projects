import * as styles from '@mui/material/styles';

export const NeonTheme: styles.Theme = styles.createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#ffffff',
        },
        secondary: {
            main: '#f50057',
        },
    },
});